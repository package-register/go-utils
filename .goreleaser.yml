version: 2

release:
  github:
    owner: package-register
    name: go-utils
  draft: false
  prerelease: false

snapshot:
  name_template: '{{ .Tag }}-next'

changelog:
  skip: true

checksum:
  disable: true

signs:
  - artifacts: none
